# Copy to .env.local and fill in. .env.local is gitignored.

# Local OWUI dev stack (matches docker-compose.yml).
OWUI_HOST=http://localhost:3000
OWUI_KEY=
OWUI_MODEL=llama3.2:1b

# Real-hardware dev — phone with Pebble companion app, developer mode enabled.
# `pebble install --phone $PHONE_IP` will use this.
PHONE_IP=

# Emulator platform name. Pebble Time 2 = emery.
# (flint = Pebble 2 Duo, gabbro = Pebble Round 2.)
EMU_PLATFORM=emery
